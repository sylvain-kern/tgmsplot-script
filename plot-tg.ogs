/*	
*	plot-tg.ogs
*	Plots the TG data
*	
*/


ty "Plotting TG data...";
ty "/!\ Make sure the baseline is imported!";
ty "--it won't plot otherwise";
ty "";

string wn$=%H;
string ms_wk$ = "ms data";
string tg_wk$ = "tg data";

page.active$ = tg data;

plotxy iy:=(col(temp),col(blsub)) plot:=202 size:=4 ogl:=1 color:= 1;	// plot vs temperature

ty "Data plotted!";
ty "";
ty "If you want to do another plot, close or minimise this one before.";

win -a %(wn$);
